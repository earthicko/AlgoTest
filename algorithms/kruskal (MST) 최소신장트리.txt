그래프의 노드를 유니온 트리로 생각한다.
노드의 개수만큼 유니온 트리 노드를 초기화한다.
간선을 가중치를 기준으로 오름차순 정렬한다.

간선을 처음부터 끝까지 하나씩 살펴본다.
    간선의 시작점과 끝점 노드 집합의 병합을 시도한다.
    시작점, 끝점의 루트를 찾는다. 이때 루트가 같을 시 사이클이 존재하는 것이므로 해당 간선은 스킵한다.
        사이클이 없을 시 두 집합을 병합한다.
